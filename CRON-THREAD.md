# Thread: the self-growing catalog

Attach `reels/promo-blocklist.png` to **reply 1** only. Main tweet is text-only.
Link goes in reply 1, not the main tweet — X throttles link-first posts.

Post all three yourself within a couple of minutes, then stay in the thread.

---

## 1 — MAIN (no image, no link)

```
My app's "New in" chip was lying.

It reshuffled a fixed catalog by date. Any returning visitor would've spotted it in two visits.

Now a GitHub Action fetches genuinely new real products every morning, commits them, redeploys.

No backend. The cron is the backend.
```

---

## 2 — REPLY  ← attach reels/promo-blocklist.png

```
First successful run added Tuborg Strong Beer and Hennessy Cognac 💀

Alcohol age-gates the whole thing on Reels/Shorts — the only algorithmic reach this project has — and sits outside GST, so my duty breakdown would've lied too.

Added a blocklist: thodasa.com
```

---

## 3 — REPLY

```
Then the blocklist had its own bug: "gin" matched Virgin Mojito.

Short keywords need both boundaries. \bgin\b, not gin\b.

Earlier wrong turn: I walked Open Food Facts' search to page 23 and every request 503'd. It caps unfiltered queries at ~5 pages.
```

---

## Notes

- **Why this framing:** the duty tweet did 674 views vs 6 on the plain product
  post. What travels is a surprising *fact*, not a feature. "My own UI was lying
  to users" and "my automation bought beer" are surprising; "catalog syncs
  daily" is a changelog line.
- The `gin` / *Virgin Mojito* line is the most shareable thing here — it's a
  regex trap most devs have hit.
- Post reply 1 immediately; the thread's value is concentrated there.
- Best windows IST: now (~1 PM lunch scroll) is decent; 9–11 PM is peak.
- If someone asks whether the products are real: yes — names, brands and pack
  photos come from Open Food Facts (ODbL), pinned at build time. Parle-G,
  Britannia, Amul, Maggi, Thums Up, Bisleri, Balaji.
